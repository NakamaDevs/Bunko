/* Entry point bundled into the documentation site by `mise run docs:codeview`.
 * Re-exports only what the page loader needs from @pierre/diffs. */
export { File, FileDiff, preloadHighlighter, DEFAULT_THEMES, DIFFS_TAG_NAME } from "@pierre/diffs";
